/*
define the entry _start globaly
define using aarch64 as the state
isr ? gdt ? idt ? some tables ? paging ? check in the armv8a what is needed for init
stack ? alignment ?
call the kernel_main at the end (pass control to kernel in Rust)
*/