[package]
name = "tests"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib"]

[dependencies]
kernel = { path = "../kernel" }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"